  * soft reset when partial mismatches same slot
  * parity checking
  * finish addon decoding
  * add scan patterns to zebraimg & zebracam
    * via some kind of reusable library API?
  * documentation
